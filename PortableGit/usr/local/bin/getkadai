#!/bin/bash
#
# Written by Yasuharu Mizutani.

usage(){
    cat <<-EOF
	
	使い方:
	  getkadai 
	
	サーバ上のkadaiディレクトリを，このPC上のホームディレクトリの下に
	あるkadaiディレクトリ（~/kadai）にコピーします．
	
	EOF
}

if [ "$#" -ne 0 ]; then
    usage
    exit 0
fi

id="`/usr/local/bin/oitname`" || exit 1
rhost="`/usr/local/bin/kadaihost`"

rsync --timeout=10 -av ${id}@${rhost}:kadai ~/

exit $?
